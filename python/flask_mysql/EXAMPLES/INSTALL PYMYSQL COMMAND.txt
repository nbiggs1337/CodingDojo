pipTo be able to connect our Flask applications to a database, we're going to need a package to help us. We're going to use one called PyMySQL. In the project level, run the following command in your command line to start a new project.






new_project_folder $ pipenv install PyMySQL flask





If needed 'cryptography' while running shell:

pip install cryptography